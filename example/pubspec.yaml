name: inject_js_example
description: A project to explain the inject_js package.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  inject_js:
    path: ..